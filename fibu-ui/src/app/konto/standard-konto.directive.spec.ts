import { StandardKontoDirective } from './standard-konto.directive';

describe('StandardKontoDirective', () => {
  it('should create an instance', () => {
    const directive = new StandardKontoDirective();
    expect(directive).toBeTruthy();
  });
});
